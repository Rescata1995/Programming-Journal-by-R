Algoritmo _1al10_
	
	num <- 0;
	// Esta variable "num" la definimos por fuera del bucle for;
	// en caso tal queramos manipularla, posteriormente,
	// por fuera del bucle mismo; es decir, conservar la variable
	// y su valor final para todo el Algoritmo.
	
	Para a<-1 Hasta 10 Con Paso 1 Hacer
		num <- num + 1;
		
		Escribir num;
	Fin Para
FinAlgoritmo
